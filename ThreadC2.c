#include <stdio.h>
#include <Thread_c.h>

int main() {
    // void run() {
    // };
    // struct Runnable runnable = Runnable.new(&run);
    // runnable.run();

    // struct Prime prime = Prime.new(10);
    // printf("test %i\n", prime.count(&prime));

    return 1;
}